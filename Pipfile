[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
start = "gunicorn -w 4 -b 0.0.0.0 'app.main:app'"
test = "pytest"

[packages]
flask = "*"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.9"
